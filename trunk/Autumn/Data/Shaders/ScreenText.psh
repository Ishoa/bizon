Texture2D tFontTexture;
SamplerState sFontSampler;

struct PSin
{
	float4	Pos : SV_POSITION;
	float4	Color : COLOR0;
	float2	TexCoord : TEXCOORD0;
};


float4 PSMain( PSin Pin ) : SV_Target
{
	return Pin.Color * tFontTexture.Sample(sFontSampler, Pin.TexCoord);
}